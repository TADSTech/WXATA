# Base URL for the WXATA backend WebSocket connection
# For local development, this is usually ws://localhost:5000
# For production, it should be the WebSocket URL of your deployed backend (e.g., wss://wxata.your-paas.com)
VITE_BACKEND_URL=ws://localhost:5000
