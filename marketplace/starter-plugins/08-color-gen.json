{
  "name": "Color Generator",
  "desc": "Generate random colors with hex, RGB, and name values",
  "trigger": "color",
  "aliases": ["colour", "randomcolor"],
  "type": "fun",
  "target": "chat",
  "response": "",
  "defaultArgument": "",
  "code": "const crypto = require('crypto');\nconst count = Math.min(parseInt(argumentName?.trim()) || 1, 5);\nlet msg = '🎨 Random Colors:\\n\\n';\nfor (let i = 0; i < count; i++) {\n  const hex = '#' + crypto.randomBytes(3).toString('hex');\n  const r = parseInt(hex.slice(1, 3), 16);\n  const g = parseInt(hex.slice(3, 5), 16);\n  const b = parseInt(hex.slice(5, 7), 16);\n  msg += `${hex} | rgb(${r}, ${g}, ${b})\\n`;\n}\nawait sendTrackedMessage(sock, remoteJid, msg);",
  "version": "1.0.0",
  "tags": ["fun", "colors", "design"]
}
